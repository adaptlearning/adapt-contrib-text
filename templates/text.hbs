<div class="text-inner">
    {{#if title}}
    <div class="text-title">
        <h4 class="text-title-inner">
            {{{title}}}
        </h4>
    </div>
    {{/if}}
    {{#if body}}
    <div class="text-body">
        <div class="text-body-inner">
            {{{body}}}
        </div>
    </div>
    {{/if}}
</div>